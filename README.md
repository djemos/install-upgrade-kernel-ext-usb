# install-upgrade-kernel-ext-usb
install-upgrade-kernel-ext-usb (script for installing kernel packages)" "install-upgrade-kernel-ext-usb is a script for installing, updating," "kernel packages on all versions of salix/slackel, including" "stable and current slackware version, without removing the " "previous kernel, in slackel installed on external usb or ssd disk.
